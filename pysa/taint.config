{
  "sources": [
    {
      "name": "UserControlled",
      "comment": "Used to annotate user-controlled input"
    }
  ],

  "sinks": [
    {
      "name": "PathTraversal",
      "comment": "Used to annotate file operations that could be vulnerable to path traversal"
    }
  ],

  "features": [],

  "rules": [
    {
      "name": "Path Traversal Vulnerability",
      "code": 5002,
      "sources": ["UserControlled"],
      "sinks": ["PathTraversal"],
      "message_format": "User controlled data may reach a file operation sink leading to path traversal"
    }
  ],

  "maximum_overrides_to_analyze": 30
}
