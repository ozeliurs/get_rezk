# Sources
flask.request.form: TaintSource[CustomUserControlled] = ...

# Sinks
def posixpath.join(path: TaintSink[PathTraversal], *paths): ...
def open(file: TaintSink[PathTraversal], mode, buffering, encoding, errors, newline, closefd, opener): ...
